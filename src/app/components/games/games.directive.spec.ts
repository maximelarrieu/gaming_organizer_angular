import { GamesDirective } from './games.directive';

describe('GamesDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new GamesDirective();
    expect(directive).toBeTruthy();
  });
});
